# Copiá este archivo a .env (git lo ignora) y poné un valor real:
#   cp .env.example .env
#
# Este archivo SÍ se commitea: documenta qué variables hacen falta, sin traer
# ningún secreto adentro. El .env de verdad nunca entra al repositorio.
DB_PASSWORD=cambiame-por-una-contrasena-local
